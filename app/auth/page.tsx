'use client';
import { FormEvent, useState } from 'react';
import { createUserWithEmailAndPassword, signInWithEmailAndPassword } from 'firebase/auth';
import { auth } from '@/lib/firebase';
import { useRouter } from 'next/navigation';

export default function AuthPage(){const [mode,setMode]=useState<'login'|'register'>('register');const [error,setError]=useState('');const router=useRouter();async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setError('');const fd=new FormData(e.currentTarget);try{if(mode==='register')await createUserWithEmailAndPassword(auth,String(fd.get('email')),String(fd.get('password')));else await signInWithEmailAndPassword(auth,String(fd.get('email')),String(fd.get('password')));router.push('/dashboard')}catch(err:any){setError(err.message)}}return <main className="shell"><div className="formCard"><div className="eyebrow">LiveOnWith.me</div><h2>{mode==='register'?'Create your account':'Welcome back'}</h2><p className="muted">Your account gives you control of memorials, contributions and privacy.</p><form onSubmit={submit}><label>Email</label><input name="email" type="email" required/><label>Password</label><input name="password" type="password" minLength={6} required/>{error&&<p>{error}</p>}<button className="button" style={{width:'100%',marginTop:24}}>{mode==='register'?'Create account':'Sign in'}</button></form><button className="button secondary" style={{width:'100%',marginTop:10}} onClick={()=>setMode(mode==='register'?'login':'register')}>{mode==='register'?'I already have an account':'Create an account'}</button></div></main>}
